const enpoint = "http://localhost:5001/";

export const endpoints = {
    jobs : `${enpoint}book/jobs`,
    grades : `${enpoint}book/grades`,
    cover : `${enpoint}book/cover`,
    menu : `${enpoint}book/menu`,
}

